name: eco_platform
channels:
  - conda-forge
  - defaults
  - gurobi
dependencies:
  - numpy
  - gurobi
  - pandas
  - pip
  - pyomo=6.2
  - python=3.*
  - scipy
  - ipython
  - matplotlib
  - mpmath
  - pint
  - entsoe-py
  - pip:
      - tsam

# conda env create -f eco_platform_env.yml
# conda remove --name eco_platform --all