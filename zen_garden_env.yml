name: zen-garden
channels:
  - conda-forge
  - gurobi
dependencies:
  - python=3.10
  - pytest
  - pip
  - numpy
  - pandas
  - geopandas
  - scipy
  - coverage
  - glpk
  - pint
  - matplotlib=3.6
  - entsoe-py
  - pyomo=6
  - gurobi
  - pip:
      - tsam
      - hdfdict
      - pyogrio
      - -e .

# conda env create -f zen_garden_env.yml
# conda remove --name zen-garden --all
