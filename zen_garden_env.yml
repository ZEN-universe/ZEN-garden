name: zen-garden
channels:
  - conda-forge
  - gurobi
dependencies:
  - pip
  - python=3.11
  - pip:
      - -e .[dev,vis,gurobipy]

# conda env create -f zen_garden_env.yml
# conda remove --name zen-garden --all
