name: zen-garden
channels:
  - conda-forge
  - gurobi
dependencies:
  - python=3.11
  - xarray
  - pytest
  - pip
  - numpy<=1.26.4
  - pandas
  - scipy
  - coverage
  - glpk
  - pint
  - matplotlib
  - gurobi
  - pytables
  - pydantic
  - requests
  - ipykernel
  - tqdm
  - ipython
  - pip:
      - tsam
      - h5py
      - filelock
      - ordered-set
      - pyogrio
      - psutil
      - linopy<=0.3.10
      - sphinx
      - myst-parser
      - uvicorn
      - fastapi
      - fastapi-cache2
      - python-dotenv
      - sqlmodel
      - python-multipart
      - -e .

# conda env create -f zen_garden_env.yml
# conda remove --name zen-garden --all
