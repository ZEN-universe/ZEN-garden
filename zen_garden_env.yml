name: zen-garden
channels:
  - conda-forge
  - gurobi
dependencies:
  - python=3.11
  - pytest
  - pip
  - numpy
  - pandas
  - geopandas
  - scipy
  - coverage
  - glpk
  - pint
  - matplotlib
  - entsoe-py
  - pyomo=6
  - gurobi
  - pip:
      - tsam
      - hdfdict
      - ordered-set
      - pyogrio
      - -e .

# conda env create -f zen_garden_env.yml
# conda remove --name zen-garden --all
