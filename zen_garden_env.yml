name: zen-garden
channels:
  - conda-forge
  - gurobi
dependencies:
  - python=3.11
  - xarray
  - pytest
  - pip
  - numpy
  - pandas
  - scipy
  - coverage
  - pint
  - gurobi
  - pytables
  - pydantic
  - requests
  - ipykernel
  - pip:
      - tsam
      - h5py
      - filelock
      - ordered-set
      - pyogrio
      - psutil
      - linopy<=0.3.10
      - sphinx
      - myst-parser
      - -e .

# conda env create -f zen_garden_env.yml
# conda remove --name zen-garden --all
