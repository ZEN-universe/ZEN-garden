name: zen-garden
channels:
  - conda-forge
  - gurobi
dependencies:
  - pip
  - python=3.12
  - pip:
      - -e .[dev,vis,gurobipy,docs]

# conda env create -f zen_garden_env.yml
# conda remove --name zen-garden --all
